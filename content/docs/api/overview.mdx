---
title: "API y webhooks"
description: "Referencia técnica: estado del arte y próximos entregables."
section: "api-referencia"
order: 1
---

Esta sección concentrará **endpoints**, **autenticación**, **eventos de webhook** y **límites** para equipos que integran E-SMART360 en sus propios sistemas.

<Callout type="warn" title="En construcción">
Estamos consolidando la documentación de **API para desarrolladores de tus clientes**, tomando como referencia la estructura de documentación de proveedores (por ejemplo **proveedor omnicanal**) y adaptándola a nuestra marca y contratos.

**Pendiente:** volcar y normalizar desde `fulldoc` y desde la consola de proveedor las rutas exactas, códigos de error y ejemplos `curl`.
</Callout>

## Qué publicaremos aquí

- Autenticación (API keys, rotación, entornos sandbox vs producción).
- Catálogo de eventos de webhook (mensaje entrante, etiqueta, pago, cita, etc.).
- Esquemas JSON y convenciones de idempotencia.

Mientras tanto, para integraciones no cubiertas aquí, abre un ticket con tu **Customer Success** o usa el canal de demo del sitio principal.
